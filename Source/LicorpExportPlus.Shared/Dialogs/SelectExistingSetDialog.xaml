<Window x:Class="LicorpExportPlus.Dialogs.SelectExistingSetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add to Existing View/Sheet Set" 
        Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="0*"/>
            <RowDefinition Height="Auto" MinHeight="50.884"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Select an existing View/Sheet Set:" 
                   FontSize="14" FontWeight="SemiBold" Margin="0,-16,5,1" Grid.ColumnSpan="2"/>

        <!-- ComboBox to select existing set -->
        <ComboBox Grid.Row="1" x:Name="ExistingSetsCombo" FontSize="13"
                  BorderBrush="#CCCCCC" BorderThickness="1"
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="Name"
                  Margin="0,1,0,16" Grid.ColumnSpan="2"/>

        <!-- Info text -->
        <TextBlock Grid.Row="2" 
                   Text="Selected sheets/views will be added to this set."
                   FontSize="11" Foreground="Gray" 
                   TextWrapping="Wrap"
                   VerticalAlignment="Top" Grid.ColumnSpan="2" Height="14"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,5,0,0" Grid.Column="1" Width="170">
            <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0"
                    IsDefault="True" Click="OkButton_Click"
                    Background="#007ACC" Foreground="White"
                    BorderThickness="0" FontSize="13" Cursor="Hand"/>
            <Button Content="Cancel" Width="80" Height="30"
                    IsCancel="True" Click="CancelButton_Click"
                    Background="White" BorderBrush="#CCCCCC" BorderThickness="1"
                    FontSize="13" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
